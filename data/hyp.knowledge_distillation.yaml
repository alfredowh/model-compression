lr: 0.0045  # initial learning rate (original SGD=0.045)
momentum: 0.9  # SGD momentum/Adam beta1
fliph: 0.0  # image horizontal flip (probability)
#weight_decay: 4e-5  # optimizer weight decay (original 4e-6)
#warmup_epochs: 3.0  # warmup epochs (fractions ok)
#warmup_momentum: 0.8  # warmup initial momentum
#warmup_bias_lr: 0.1  # warmup initial bias lr
ce_weight: 0.75 # cross-entropy loss weight only by knowledge distillation
kd_loss_weight: 0.25  # knowledge distillation loss weight only by knowledge distillation
